<!DOCTYPE html>
<html lang="en">
    <head>

        <title>TinyApp</title>

        <style>
            table {
                color: #333;
                font-family: Helvetica, Arial, sans-serif;
                width: 840px;
                border-collapse: collapse;
                border-spacing: 0;
            }
            td, th {
                border: 1px solid #CCC;
                height: 30px;
            }
            th {
                background: #F3F3F3;
                font-weight: bold;
            }
            td {
                background: #FAFAFA;
                text-align: center;
            }
        </style>
    </head>
    <body>
        <%- include ../partials/_header %>
        <div align="center">
        <table margin=4>
            <th>Short URL</th>
            <th>Original URL</th>
            <th>Redirects</th>
            <th>Unique Visits</th>
            <th>Edit?</th>
            <th>Delete?</th>
        <% for (url of urls) { %>
            <tr>
                <td>localhost:8080/u/<b><%= url.id %></b></td>
                <td><%= url.url %></td>
                <td><%= url.redirects %></td>
                <td><%= url.getTotalUniqueVisitors() %></td>
                <td><a href="/urls/<%= url.id %>">Edit!</a></td>
                <td><form method="post" action="/urls/<%= url.id %>?_method=DELETE"><input type="submit" value="Delete!"></form></td>
            </tr>
        <% } %>
        </table>
        <p><a href="/urls/new">New Short URL!</a></p>
        </div>
    </body>
</html>