<!DOCTYPE html>
<html lang="en">
    <%- include ../partials/_head %>

    <body style="font-size:20px; padding:50px;">
        <%- include ../partials/_header %>
        <form action="/urls/<%= url.id %>?_method=PUT" method="POST" style="margin:150px;">
            <h2><label for="longURL">Update existing URL: <%= url.id %></label></h2>
            <input id="longURL" type="text" name="longURL" value="<%= url.url %>" style="width: 300px">
            <input type="submit" value="Submit">
        </form>
        <br><br>
        <table margin=4 align="center">
            <th>Visitor ID</th>
            <th>Timestamp</th>
            <% for (visit of url.uniques) { %>
            <tr>
                <td><%= visit[0] %></td>
                <td><%= new Date(visit[1]).toLocaleString() %></td>
            </tr>
            <% } %>
        </table>
    </body>
</html>
