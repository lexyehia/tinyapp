<!DOCTYPE html>
<html lang="en">
    <%- include ../partials/_head %>

    <body>
        <%- include ../partials/_header %>
        <div align="center">
        <h2>Edit Your Account</h2>
        <form action="/users/<%= user.id %>?_method=PUT" method="POST" style="margin-top:150px;">
        <div class="form-group">
            <label for="email">Email: </label>
            <input id="email" type="email" name="email" value="<%= user.email %>" style="width: 300px" class="form-control">
        </div> 
        <div class="form-group">
            <label for="password">Password (leave blank if not changing it): </label>
            <input id="password" type="password" name="password" style="width: 300px" class="form-control">
        </div>
        <input type="submit" value="Make Changes" class="btn btn-success btn-lg"></form>
 <form action="/users/<%= user.id %>?_method=DELETE" method="POST" style="margin:20px;"><input type="submit" value="Delete Account" class="btn btn-danger btn-lg"></form>
        </div>
    </body>
</html>